# CP Relay Server

Relay server for Canon Protocol (CP).

## Overview

CP Relay Server provides:
- HTTP API for document sync
- Push/pull endpoints
- Connection management

## Usage

```bash
cargo run -p cp-relay-server
```

The server runs on port 8080 by default.
